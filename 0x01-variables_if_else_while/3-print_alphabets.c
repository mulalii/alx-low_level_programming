#include <stdio.h>
/**
 * main - entry point
 * Description: run alphabet
 * Return: Always (success)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch != 'e' && c != 'q')
		{
			putchar(ch);
		}
	}
	putchar('\n');
	return (0);
}
