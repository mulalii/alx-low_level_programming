#include "main.h"
/**
 * _puts - function that prints a string followed by new line
 * @str: variable
 */
void _puts(char *str)
{
	for(str = 0; str[i] != '\0'; str++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
